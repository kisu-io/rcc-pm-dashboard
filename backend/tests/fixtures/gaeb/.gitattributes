# GAEB exchange files are compared byte for byte by the tests and are
# regenerated by a script; never apply line-ending normalisation to them or a
# checkout on another platform stops matching what the generator wrote.
*.x81 -text
*.x82 -text
*.x83 -text
*.x84 -text
*.x85 -text
*.x86 -text
